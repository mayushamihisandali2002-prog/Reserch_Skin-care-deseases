# ── Core web framework ────────────────────────────────────────────────────────
flask
flask-cors
gunicorn

# ── Environment configuration ─────────────────────────────────────────────────
python-dotenv

# ── Data / ML utilities ───────────────────────────────────────────────────────
numpy
pandas
scikit-learn
joblib

# ── Deep Learning — CPU-only PyTorch (CRITICAL: prevents 4GB CUDA download) ──
# Must come BEFORE other packages. Forces the small CPU wheel (~200MB not 4GB).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu
timm

# ── HuggingFace Transformers — loads best_model/best_model/ (DistilBERT) ──────
transformers
# safetensors is needed to load model.safetensors weight files
safetensors

# ── Image handling ────────────────────────────────────────────────────────────
Pillow

# ── Audio/Speech Recognition ──────────────────────────────────────────────────
# SpeechRecognition for transcribing uploaded audio files
SpeechRecognition
# Pydub for audio format conversion (requires ffmpeg installed on system)
pydub
# Bundled ffmpeg binary fallback used by pydub when system ffmpeg is unavailable
imageio-ffmpeg

# ── Database (Supabase) ────────────────────────────────────────────────────────
supabase

# ── External AI APIs ────────────────────────────────────────────────────────────
# Groq - free ultra-fast inference (recommended!)
groq
# Google Gemini - for image analysis
google-generativeai

opencv-python-headless
